Found while deciding where to put mf's own dogfooding field: mf/indexer.py's _SKIP_DIRS (.git, .venv, venv, __pycache__, node_modules, .mfgpt, raw) does not include eval/, and the MF repo's own eval/corpus/{codebase,papers} are 157 real frontmattered memoryfield pages (the calibration fixtures). Running mf init/mf index at the MF repo root would silently sweep all 157 calibration pages into whatever field lives there, polluting real search results with synthetic eval content. Also found two untracked, unreferenced top-level directories, corpus/ and queries/, dated Aug 31 (older than eval/corpus's Aug 31 11:14) and not in git ls-files -- look like leftovers from before the 1.1 harness/ -> eval/ rename that never got cleaned up. Resolution: don't init a field at the repo root; put mf's own operational memory in a dedicated notes/ subdirectory instead, with its own .claude/settings.json so hooks only fire for a session actually opened at notes/. The stray corpus/ and queries/ dirs are untouched -- worth a separate look, not cleaned up as part of this task per the concurrent-session caution in CLAUDE.md gotcha 24.
