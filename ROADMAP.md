# mf — tasklist and phases

State as of this writing: **Phase 0 is closed except 0.5** (upstream frontmatter proposal, blocked on Cal's contact info — needs a human). **Phase 1 is closed (1.1-1.9 all done).** **Phase 2 is underway: 2.1 done, 2.2-2.4 not started.** `mf init`, `mf index`, `mf search`, `mf read`, and `mf write` are real, tested (112/112), and verified end to end against sqlite-vec + fastembed and the full 157-page corpus, not just mocked. Two of the plan's literal specs didn't survive contact with real calibration and had to be redesigned, not just implemented: the score gate (1.4) needed a normalized-bm25-floor-plus-FTS/dense-agreement combination instead of the plan's single floor+gap heuristic, which doesn't separate no-answer from real-answer queries on this corpus; and `search` (1.5) now runs dense on every query rather than as a true fallback, because the gate's agreement signal needs it every time (CLAUDE.md gotcha 13, resolved). Testing 1.5 against the real corpus (not synthetic fixtures) also found and fixed two frontmatter-parser bugs (CLAUDE.md gotcha 19) that were silently rejecting most real pages. `read` (1.6) adds a `reads` log table and is the only path that populates `co_read` weight in `links`, closing the gap `search`'s neighbor code has documented as a no-op since 1.5. `.claude/skills/mf/SKILL.md` (1.7) documents only what's built. 1.8's blind query set found the calibrated confidence gate's "0% false-high" claim doesn't survive vocabulary mismatch (gotcha 25). 1.9's real-agent trial found `mf search`'s default flags cost *more* tokens than raw exploration for a point lookup, and that the tool only delivers PLAN.md section 6's modeled savings when called leanly (gotcha 26) — both real, load-bearing findings from actually measuring instead of assuming. `write` (2.1) adds the dedup gate (`vec` table's second job) with an explicitly uncalibrated first-cut threshold, not yet a `mf/confidence.py`-grade constant. Open debt: 0.5's upstream email still unsent, 2.1's dedup threshold still uncalibrated — the eval numbers above are all still conditional on a corpus authored alongside its own query set (CLAUDE.md gotcha 7). `docs/architecture.md` holds the standing schema/retrieval reference, kept in sync with each phase as it lands.

Ordering rule carried over from the killed-process incident: compute time and uncommitted state never overlap. Anything marked (commit) lands before the next long-running task starts.

---

## Phase 0 — close out M0.5 (half a day)

- [x] **0.1 Commit pending bug-fix batch** (commit). Done across `67e4a2b`..`7c724a8`; superseded-headline framing landed in the M0.5 report rather than a commit message, which reads fine in context.
- [x] **0.2 Run the two remaining rows** — `dense_bge_papers`, `hybrid_papers`. Both present in `eval/results/` and `summary.json`. Confirmed moot at ceiling as expected.
- [x] **0.3 Write REPORT.md** from report.py output plus narrative. Required structure, checked against the current [M0.5_REPORT.md](M0.5_REPORT.md):
  - Headline: what you embed matters more than which model (the two swing numbers). Present.
  - Second: ceiling caveat. Present.
  - Third: scope condition (write discipline, lint load-bearing). The lint-load-bearing framing lives in [docs/architecture.md](docs/architecture.md) and CLAUDE.md gotcha 16 rather than duplicated inline in the report, consistent with the report's own no-duplication convention.
  - "What changed and why" section. Present.
  - Findings 5 and 7 fixed. Present; finding 7 ("hybrid has nothing left to fuse") reflects the post-fix data where hybrid is no longer a net loss.
  - All tables with CIs; stub-end reported conditional on hit; provisional rows marked. **Was not actually true until this pass** — `stub_end_given_hit_rate` incremented its hit counter unconditionally on every retrieval hit, so it evaluated to a flat `1.0` in all 12 result files and the report used the ungated `stub_end_rate` instead. Fixed in `run_baselines.py`, all 12 rows rerun, CIs and the corrected given-hit rate (0.69–0.87 across baselines) now render in the report tables. See CLAUDE.md gotcha 17. No provisional rows remain; all 12 baseline×domain combos are complete.
- [x] **0.4 Stub-end de-bias check.** De-biased judgments exist (`eval/stub_sufficiency_debiased.jsonl`, 214 entries matching the paraphrased set) and are wired into the report. Spot-check of 20 (random sample, seed 42) done: a fresh subagent judged query+stub_text blind, with no page body and no visibility into the recorded label or this conversation's earlier reads of the file. Result: 18/20 (90%) agreement; both disagreements were the spot-check judge reading the sufficiency bar more strictly (stub gestures at the topic vs. stub states the specific fact asked for). Artifact: `eval/stub_sufficiency_spotcheck.json`; rendered in [M0.5_REPORT.md](M0.5_REPORT.md) via `report.py`'s `spotcheck_section()`. 90% agreement on a 20-sample spot-check is a reasonable bar to clear, not a tight one — treat the de-biased set as validated-enough-to-cite, not as re-verified end to end.
- [ ] **0.5 Upstream frontmatter proposal to Cal** — `status`, `supersedes` (uuid), `contradicts`, `depends_on`, `source`, `writer`, `tags`. Draft written (see session transcript); blocked on Cal's contact info or spec-repo link, which isn't in this repo. Needs a human to supply the destination and send it.

## Phase 1 — M1 read path (the tool proper; ~1–2 weeks)

- [x] **1.1 Repo restructure** (commit). `mf/` package created (`mf/__init__.py`, `mf/cli.py`) with a stub console entry point for `init`/`index`/`search`/`read`/`write` (each exits 1 with a "not implemented" message); `pyproject.toml` added, verified installable via `uv tool install .`. `harness/` renamed to `eval/` (all internal imports, doc references, and `pyrightconfig.json` updated; verified `ruff check eval/`, `npx pyright eval/`, and a regenerated `M0.5_REPORT.md` all still pass). Eval's dependencies (`fastembed`) are now a `pyproject.toml` optional-dependency group (`mf[eval]`, `uv sync --extra eval`) instead of the undeclared external `~/.hermes/hermes-agent/venv`; verified end-to-end with `uv run python3 -m eval.run_baselines --baseline fts --domain codebase`. **Not done:** `eval/run --check` "regression suite runnable against any build" — there's no `mf` build to check yet (that's 1.3+), so this is deferred rather than faked. Fixed a real bug found along the way: CLAUDE.md gotcha 10's summary.json recovery procedure was wrong (see corrected gotcha 10).
- [x] **1.2 `embedding_text()` as single source of truth.** `mf/embedding.py`: `embedding_text()` (title+summary+L1), `document_text()`/`query_text()` (model-correct task prefixes for nomic/bge), unit-tested (`tests/test_embedding.py`). `mf/query_prep.py`: `fts_query()`, the shared FTS5 MATCH-expression builder (hyphen/quote-safe tokenization, stopword OR-join, plus a `dropped` token list a caller can log for phrase-loss visibility — new, wasn't tracked before), unit-tested (`tests/test_query_prep.py`). `eval/baselines/dense_real_baseline.py`, `fts_baseline.py`, and `hybrid_baseline.py` now import these instead of each building their own text/query strings, closing the exact gap gotchas 2 and 3 describe. **Verified behavior-preserving**, not just "should be equivalent": reran `fts`, `hybrid`, and `dense_nomic` on the codebase domain post-refactor and diffed against the pre-refactor committed results — P@3/R@5/MRR identical to three decimal places on all three (only `elapsed_seconds` and the self-referential `details` path differ). 14/14 tests pass; `ruff`/`pyright` clean (same 7 pre-existing `report.py` findings, unrelated). Added `pytest` as a `[dependency-groups] dev` dep — this repo's first test suite. Note for future runs: `uv sync --extra eval` and `uv sync --group dev` don't compose across separate calls; pass both flags together (documented in README.md).
- [x] **1.3 Schema + `mf init` + `mf index`.** `mf/schema.py`: all six tables (`pages, sections, fts, vec, links, claims`) plus `config` (model_code/embedding_dim/schema_version — the "never conflate model versions" requirement). `mf/page.py`: a real YAML frontmatter parser for the full spec (uuid, title, created, updated, summary, status, supersedes, contradicts, depends_on, tags, source, writer), section splitting into slugged L1/L2 per `docs/architecture.md`. `mf/db.py`: `init_field()`/`open_field()` over sqlite-vec-loaded connections; `mf init` refuses to overwrite an existing `mf.sqlite3`. `mf/indexer.py`: `mf index` walks a field directory (any `.md` that fails to parse as a page is silently not one — no name denylist), incremental on sha256 (unchanged pages are skipped entirely, no re-embed), removes index rows for pages whose file disappeared, and re-embeds via fastembed (nomic) using `mf/embedding.py`'s `document_text()` from 1.2. `supersedes`/`contradicts`/`depends_on` frontmatter populate `links` at index time. Wired into `mf/cli.py`'s `init`/`index` subcommands (both operate on cwd by default, or an optional dir argument). 53/53 tests pass (24 new: schema/db/page/indexer/cli). Added `sqlite-vec`, `fastembed`, and `pyyaml` as real `mf` production dependencies (not eval-only), per an explicit ask-first check. **Still not built, now genuinely overdue:** `search`'s `refuse-on-stale unless --stale-ok` — PLAN.md's stale-embeddings row (§1 table) assigns that specifically to `search`, not `index`. `mf/search.py` (1.5) shipped without it: it never checks a page's on-disk sha256 against what's indexed before returning a result. `index` already computes and stores the sha256 `search` needs for that comparison; this is unclaimed work for whoever picks up `search` next, not blocked on anything. **Note:** built alongside a second, concurrent session that added an MLX (Apple Silicon) embedding backend (`mf/embed_backend.py`, `eval/baselines/dense_mlx_baseline.py`) in the same window — no file collisions, but `pyproject.toml`/`README.md`/`eval/run_baselines.py` were left for that session to commit since both sessions were touching them.
- [x] **1.4 Score gate calibration.** The literal plan spec (a single floor + relative-gap heuristic on raw FTS bm25) doesn't work: no-answer and real-answer-correct-hit score ranges overlap almost completely on this corpus (gotcha 7's ceiling effect again). What works instead, calibrated against the 30 no-answer queries via `eval/calibrate_confidence.py`: a combined gate, `mf/confidence.py`. Normalized bm25 (raw score / matched-term count) decides none vs. not-none — raw magnitude mostly tracks query length, not match quality. FTS/dense top-1 agreement decides high vs. low — 97.0% agreement on genuinely correct hits, 16.7% on no-answer queries, a far cleaner signal than any score threshold tried. At the chosen floor (2.0): 0% false-high-confidence on the no-answer set, 19.8% of correct hits return `none` instead of the right answer (a real, accepted cost — see the trade-off table in `eval/calibrate_confidence.py`, which also serves as the "false-answer-rate as a first-class harness metric" this task asked for). 10 new unit tests. Caught and fixed a real scale-mismatch bug mid-calibration that had produced a false "0% false-high at floor=1.5" result before the fix — see CLAUDE.md gotcha 18. **Now wired in:** 1.5's `mf search` calls `mf/confidence.py` on every query. Resolved the per-query-cost question this entry originally flagged as open: dense runs on every query (not a true fallback) so the agreement signal is always available — see 1.5's entry and CLAUDE.md gotcha 13.
- [x] **1.5 `mf search`.** `mf/search.py`, wired into `mf/cli.py`. Design changed from the original plan per 1.4's finding: dense now runs on every query (not only as a fallback when FTS is empty), since the calibrated confidence gate needs the FTS/dense agreement signal on every call. Dense's own top-k is used as the actual result set only when FTS finds nothing at all (the `vec` table's fallback-ranking job, docs/architecture.md). Stubs (uuid, title, summary, status, tokens) + neighbor stubs (typed links, then query-time kNN; co_read is a documented no-op until `mf read`/1.6 exists to populate it) + `--budget` token cap (drops neighbors before dropping whole stubs) + `--json`. Superseded pages fold into a `{uuid, superseded_by}` pointer instead of a full stub. 24 new tests (9 `mf/search.py`, 5 CLI-level), all passing, verified against a real 157-page corpus (both eval domains) with real sqlite-vec + fastembed, not just mocked.

  Found and fixed two real parser bugs along the way, surfaced only by testing against the *actual* corpus instead of a synthetic one: `mf/page.py`'s strict-YAML frontmatter choked on this project's own `"Topic: specific question"` title convention (an unescaped `": "` reads as a nested YAML mapping) and on values starting with a backtick (an inline-code term opening a summary) -- both silently swallowed by `discover_pages()`'s `PageParseError` catch, so `mf index` reported "0 upserted" with no indication why. Fixed by quoting every frontmatter scalar value before handing the block to `yaml.safe_load()`, rather than asking the corpus to requote itself. All 157 pages across both eval domains now parse.

  Also surfaced (not a bug, a scale property worth recording): raw bm25 magnitude is corpus-size-dependent -- the same normalized-score floor (2.0) that works on a 75+ page corpus reads as permanently "none" on a 4-page toy field, since bm25's IDF term shrinks with fewer documents. Confirmed by testing search end-to-end against both a synthetic 4-page field and the real 157-page corpus before trusting the result.
- [x] **1.6 `mf read`.** `mf/read.py`, wired into `mf/cli.py`. `read uuid[#section] --tier L1|L2`, one or more refs per call. A bare uuid defaults to `--tier L1` (first section); `--tier L2` is everything after L1, concatenated (empty on a single-section page); a `uuid#slug` ref returns that named section regardless of `--tier`. Every successful read is logged to a new `reads` table (id, uuid, section, tier, read_at); a multi-ref call bumps `co_read` weight in `links` for every pair of uuids read together, canonical `src < dst` ordering so weight accumulates on one row across repeated calls instead of splitting across both directions — this is the only path that populates `co_read` (search's neighbor kinds are typed links and query-time kNN, computed fresh each call, never stored). Page content is never cached in the index: each read re-parses the page fresh off disk via the same `mf.page.load_page()` parser `mf index` uses, keyed by the `filename` column already in `pages`. 20 new tests (`tests/test_read.py`, `tests/test_cli_read.py`), 99/99 passing; `ruff`/`pyright` clean (same 7 pre-existing `report.py` findings). Verified against the real 157-page corpus, not just the test fixtures: L1/L2 tiers, a named `#section` ref, the not-found error path, and `co_read` weight accumulating to 2.0 across two calls of the same pair.
- [x] **1.7 SKILL.md v0.** `.claude/skills/mf/SKILL.md`, per PLAN.md section 8's layout. Covers exactly what's built (`mf search`, `mf read`, `mf index`) and is explicit that `write`/`raw add`/`lint` don't exist yet rather than documenting the aspirational full command set. Search-before-explore; how to read `search`'s `confidence: high|low|none` field, since a `none`-confidence stub shouldn't be cited as an answer; stub-first reading (the summary is written to be the answer, so a `read` call often isn't needed); when to escalate L1 -> L2 -> a named `#section`, and when to batch multiple refs into one `mf read` call for the `co_read` signal; the writing conventions from docs/architecture.md, condensed, for hand-authoring a page in the absence of `mf write`/`lint`. A "Don't" section calls out not citing `none`-confidence results, not defaulting to L2, not reaching for the unbuilt write-path commands, and not bypassing `mf read` with a direct file read (which silently loses the `co_read` signal). This is the skill 1.9's real-agent trial is meant to test against the raw field.
- [x] **1.8 Blind vocabulary-mismatch query set.** 48 queries (40 real-answer + 8 no-answer, 24 per domain), split into `eval/queries/{codebase,papers}/queries_blind.jsonl`. Methodology: task/scenario descriptions were written with full corpus knowledge (needed to know the ground-truth answer uuid, or, for the 8 no-answer queries, to confirm the topic genuinely isn't covered), but the actual query *text* was generated by 6 fresh subagents given only the neutral scenario, explicitly instructed not to read any files -- genuinely blind to the corpus's own vocabulary, matching the plan's "describe the task, get the question." `eval/run_baselines.py`'s `DOMAINS` gained `codebase_blind`/`papers_blind` entries (same corpus, different queries file) so the full 6-baseline matrix could run against it without ever touching the M0.5 `queries.jsonl` or `results/summary.json` (`git checkout`'d after every individual run, matching gotcha 10's safe pattern) -- separate `results/{baseline}_{domain}_blind.json` files, reassembled into `results/blind_summary.json` by the same read-`["metrics"]`-per-file method gotcha 10 documents, not by trusting summary.json's overwrite-per-run behavior.

  Headline (P@3, blind vs. M0.5, n=24/domain so treat deltas near +/-0.05 as noise): grep 0.827->0.800, fts 0.952->0.925, dense_tfidf 0.893->0.825 (the biggest drop -- consistent with gotcha 5, TF-IDF isn't real dense retrieval and it shows here), dense_nomic 0.977->0.975, dense_bge 0.984->1.000, hybrid 0.983->0.975. Real embedding models are flat to slightly up under vocabulary mismatch; lexical/TF-IDF methods degrade, most visibly TF-IDF.

  The literal question this task asked -- does the dense fallback (`mf/search.py`'s FTS-empty branch) ever fire -- can't be answered by the baseline matrix at all: every baseline's top-k always returns k (gotcha 6), so none of them ever hit "FTS found nothing." Answering it needed a second script, `eval/blind_fallback_check.py`, running the real `mf search` pipeline directly. Result: the fallback **still essentially never fires** -- 0% on both blind sets, same as the 0-1% baseline rate on the original query sets -- because `fts_query()`'s OR-joined tokenization finds *some* lexical overlap almost every time, even under intentionally mismatched phrasing. The bm25-floor gate is what actually absorbs the vocabulary-mismatch degradation instead: correct-hit demotion to `confidence: none` roughly doubled versus M0.5's calibrated 19.8% baseline (gotcha 15) -- 45% of real-answer blind queries in both domains came back `none` -- and `mf search`'s own top-1 accuracy dropped from 0.828->0.700 (codebase) and 0.882->0.800 (papers).

  **New finding, not just a number: the calibrated "0% false-high-confidence" claim (gotcha 15/18) does not hold under vocabulary mismatch.** `blind-code-na-004` ("GDPR deletion request process for customer data" -- a genuine no-answer query, no page in the corpus covers this) returned `confidence: high` pointing at `code-dm-soft-delete`, a topically-adjacent but wrong page. n=1/8 no-answer blind queries, so this needs a larger blind no-answer sample before it's a number to calibrate against, but it's a real, reproduced instance, not noise -- see CLAUDE.md gotcha 25. Recalibrating the gate against a proper blind no-answer set is follow-up work, out of scope for this task.

  Artifacts: `eval/queries/{codebase,papers}/queries_blind.jsonl`, `eval/results/*_blind.json` (12 files) + `eval/results/blind_summary.json`, `eval/blind_fallback_check.py`. `ruff`/`pyright` clean at the existing baseline; `mf`'s own 99/99 tests unaffected (eval-only change).
- [x] **1.9 Real-agent trial.** N=20 tasks x 2 conditions (40 real Claude subagent runs, general-purpose, real tool access): one agent with `mf` + the SKILL.md guidance vs. the same agent given the raw codebase-domain corpus with no index. Full methodology, per-task transcripts, and findings: `eval/agent_trial_1_9.md`; the deterministic token-cost half is reproducible via `eval/agent_trial_token_costs.py`.

  Correctness 40/40. Stub-end rate (condition A) 100% -- every task answered from the top search stub alone, zero `mf read` calls. Wrong-page reads: 0/20 (condition A, no reads at all) vs 2/20 (condition B, both topically-adjacent near-misses before the right file). The Agent tool's own `subagent_tokens` figure turned out to be useless for this comparison -- both conditions averaged ~51,900-52,000, a 0.04% difference, because ~50k of fixed per-agent-session overhead swamps the actual few-hundred-token retrieval difference. Isolating the content tokens each condition actually put in context instead:

  **Central finding: `mf search`'s *default* flags cost more tokens than raw file exploration for a single-answer point lookup.** Default (`--limit 5 --neighbor-limit 3`): 1014 tokens/task average, 5.85x *more* than raw's 173/task, because every one of these 20 tasks needed only the single top stub but the default renders 5 top-level stubs plus up to 3 neighbors each regardless. Lean (`--limit 1 --neighbor-limit 0`): 55 tokens/task, 3.2x *less* than raw -- the shape that actually delivers PLAN.md section 6's modeled savings. `.claude/skills/mf/SKILL.md` (1.7) updated with this guidance (call `mf search` lean for a point lookup, widen only on low/none confidence or a genuinely broad question); `mf/search.py`'s `DEFAULT_LIMIT`/`DEFAULT_NEIGHBOR_LIMIT` constants themselves are untouched -- changing them also touches 1.4's confidence-gate calibration (run at `limit=5`), so that's separate, deliberate follow-up work, not bundled into this measurement task.

**Phase 1 exit: closed.** All three criteria are now met -- search+read work end-to-end on the eval field (1.5/1.6, real corpus, real sqlite-vec + fastembed); the score gate has numbers (1.4's calibration, with 1.8's vocabulary-mismatch caveat now attached, CLAUDE.md gotcha 25); the savings table is measured, not modeled (1.9, with the caveat that the measurement depends on calling `mf search` leanly, CLAUDE.md gotcha 26). Only 0.5's unsent upstream email remains open debt from Phase 0/1 as a whole -- it still needs a human to supply Cal's contact info.

## Phase 2 — M2 write path (~1 week)

- [x] **2.1 `mf write`.** `mf/write.py`, wired into `mf/cli.py`: `write <path> [--field DIR] [--update UUID] [--force] [--json]`. The page must already exist on disk inside the field (`write` validates and commits, it doesn't create the file). Validates frontmatter via the same `mf.page.load_page()` parser `index` uses, then runs the dedup gate -- embeds the page and kNN-queries `vec` for existing pages within `DEDUP_THRESHOLD` (7.0, L2 distance), excluding the page's own uuid so a self-update never dedup-blocks itself. A hit returns the candidates and exit code 2 without writing; `--update UUID` (must match the page's own uuid) or `--force` skip the gate. On a pass, `write` calls the same `indexer.index_field()` `mf index` uses -- no separate `index` call needed after a successful `write`.

  `DEDUP_THRESHOLD` is a first-cut estimate, not a calibrated constant like `mf/confidence.py`'s `FLOOR`: one synthetic probe put a paraphrased near-duplicate of a real corpus page at L2 distance ~4.9 from the original, against ~10.2 for the nearest genuinely-different-but-related page -- a wide, cleanly-separated gap on that single example, not a proper calibration (that would need a labeled near-duplicate set the way `eval/calibrate_confidence.py` had a labeled no-answer set for 1.4, which doesn't exist yet). 13 new tests (`tests/test_write.py`, `tests/test_cli_write.py`), 112/112 passing; `ruff`/`pyright` clean at the existing baseline. Verified against the real 157-page corpus, not just mocked vectors: a genuinely new page wrote cleanly, a hand-written paraphrase of a real page (`code-deploy-rollback-cmd`) was correctly blocked at distance 5.74, and `--force` correctly overrode it. `.claude/skills/mf/SKILL.md` updated -- it previously said `mf write` didn't exist and told agents to hand-edit pages then `mf index`; it now teaches `mf write <path>` as the write path and documents the exit-code contract (0 written, 1 invalid input, 2 dedup-blocked).
- [ ] **2.2 `mf raw add`** appending to non-indexed `raw/`; prefix-dedup for double session-end fires.
- [ ] **2.3 `mf lint`** enforcing plan §5: token budgets, summary-shape heuristic, `## Don't` placement, copied-state patterns (SHAs, counts, relative dates), missing `source`, superseded-but-active, orphans, hash mismatches. `--check` exit code for CI.
- [ ] **2.4 `mf pack` / `unpack`** with sha256; round-trip compatibility test against a spec-plain memoryfield (no extended fields) and Cal's zip layout.

## Phase 3 — M3 hooks and imports (~3 days)

- [ ] **3.1** Claude Code SessionEnd hook → `mf raw add --from-transcript`; CLAUDE.md/AGENTS.md two-line snippet.
- [ ] **3.2** `mf import claude-memory <dir>` (MEMORY.md lines → stubs, topic files → pages) and `mf import wiki <dir>` (index.md entries → summaries, flatten subdirs).
- [ ] **3.3** CI recipe: `mf lint --check` pre-commit; post-commit `mf index`.

## Phase 4 — M4/M5 conditional features

- [ ] **4.1 Reranker eval** — gated on 1.8's blind set. If FTS+gate+dense-fallback holds P@3 there, the reranker is cut, not deferred.
- [ ] **4.2 `mf consolidate --plan`** emitting create/update/supersede JSON from `raw/` with evidence; executed by the host agent via `write`.
- [ ] **4.3 Multi-writer:** `mf claim` (atomic conditional insert), `contested` status, consolidation dedup pass.
- [ ] **4.4 co_read weighting** in neighbor ranking once enough signal accumulates.

## Phase 5 — M6 surfaces and packaging

- [ ] **5.1 MCP server** wrapping search/read/write/raw_add, same JSON contract.
- [ ] **5.2** Packaging polish; Rust port only if install friction is the top user complaint.

---

## Code-review checklist (pending re-upload; also usable as self-audit)

The review I'd run on the harness code, given this project's history:

1. **Single sources of truth.** Is embedding-text construction one function or copy-pasted per baseline? Same for query preprocessing, token counting, and the tokenizer used for budgets — drift here caused the two biggest bugs so far.
2. **Checkpointing.** Does each baseline row write its own result file on completion, or does everything funnel through one summary.json that a kill mid-run corrupts? (This bit once already.)
3. **Determinism and provenance.** Are paraphrase-generation prompts, model versions, and seeds recorded next to the outputs? Can the query sets be regenerated or are they one-of-a-kind artifacts that must never be lost? If the latter, are they committed?
4. **CI math.** Bootstrap implementation — resampling queries (right) vs. resampling hits (wrong); interval on the difference between methods, not just per-method intervals.
5. **Prefix/config hygiene.** Model-specific query/document prefixes in config next to the model name, not hardcoded in a baseline file where the next model swap silently drops them.
6. **Metric conditioning.** Stub-end computed given-hit everywhere, or still mixed in places; no-answer scored as abstention (needs gate) vs. empty-top-k (impossible). **Checked this pass:** was mixed (computed but silently broken, see 0.3), now fixed and consumed by the report.
7. **Hardcoded paths, temp dirs, and whether `eval/` runs from a clean clone.**
8. **Tests.** Anything at all around the FTS query sanitizer and the corpus builder — the two components whose silent failures shaped early conclusions.
9. **Dead code from the flip** — summary-only embedding paths, the unprefixed BGE variant, TF-IDF-labeled-as-dense remnants; delete or mark superseded so the next reader doesn't resurrect a buggy baseline.
10. **README/report drift** — do the docs in the repo still carry the stale "FTS wins" narrative anywhere outside git history?