"""Map code page titles to their query-side slug ids.

This is the single source of truth for what id each code page gets in
queries. Each entry was hand-verified against CODE_QUERIES to make sure
the slug id used in queries matches a real page.
"""
# Format: title-prefix (case-insensitive) -> slug id
# Each title in CODE_PAGES starts with one of these prefixes.
TITLE_TO_SLUG = {
    "Auth: rotating the JWT signing key": "auth-rotate-jwt-key",
    "Auth: why we use Ed25519 not RSA": "auth-ed25519-vs-rsa",
    "Auth: how service-to-service tokens work": "auth-s2s-tokens",
    "Auth: handling a leaked token": "auth-leaked-token",
    "Auth: the difference between user JWT and service JWT": "auth-user-vs-service-jwt",
    "Auth: how OAuth callback validates state": "auth-oauth-state",
    "Auth: what `aud` claims mean in practice": "auth-aud-meaning",
    "Auth: where the public keys live": "auth-public-keys",
    "Auth: what the token `kid` header means": "auth-kid-meaning",
    "Auth: why we don't use sessions": "auth-no-sessions",
    "Billing: how invoice lines are computed": "billing-line-math",
    "Billing: why we don't store currency conversions": "billing-no-fx-storage",
    "Billing: the proration edge case at month boundaries": "billing-proration-edge",
    "Billing: handling refunds": "billing-refunds",
    "Billing: what `tax_mode: inclusive` means": "billing-tax-inclusive",
    "Billing: how trial periods are billed": "billing-trial",
    "Billing: idempotency key for invoice creation": "billing-idempotency",
    "Billing: how dunning works": "billing-dunning",
    "Billing: the payout reconciliation job": "billing-payout-recon",
    "Billing: stripe webhook signature verification": "billing-stripe-webhook",
    "Migrations: how to add a NOT NULL column safely": "migrations-add-not-null",
    "Migrations: online vs offline schema changes": "migrations-online-offline",
    "Migrations: why we never drop a column in the same release": "migrations-never-drop-same-release",
    "Migrations: the `lock_timeout` setting": "migrations-lock-timeout",
    "Migrations: testing a destructive change": "migrations-test-destructive",
    "Migrations: how to rename a column safely": "migrations-rename-column",
    "Migrations: the audit log table": "migrations-audit-log",
    "Migrations: how to roll back": "migrations-rollback",
    "Migrations: zero-downtime schema changes cheat sheet": "migrations-zero-downtime",
    "Migrations: the difference between `up` and `expand`": "migrations-up-vs-expand",
    "Deploy: the canary window": "deploy-canary-window",
    "Deploy: how to roll back a bad release": "deploy-rollback-cmd",
    "Deploy: why we use immutable image tags": "deploy-immutable-tags",
    "Deploy: the deploy freeze calendar": "deploy-freeze-calendar",
    "Deploy: feature flag rollout order": "deploy-feature-flags",
    "Deploy: pre-deploy checklist": "deploy-pre-checklist",
    "Deploy: how pod disruption budgets work": "deploy-pdb",
    "Deploy: the difference between a deploy and a release": "deploy-vs-release",
    "Deploy: how to debug a stuck rollout": "deploy-stuck-rollout",
    "Deploy: the deploy log format": "deploy-log-format",
    "Observability: the SLO for billing endpoints": "obs-slo-billing",
    "Observability: how traces are sampled": "obs-trace-sampling",
    "Observability: the alerting severity ladder": "obs-alert-severity",
    "Observability: what `trace_id` propagation requires": "obs-trace-id-prop",
    "Observability: dashboard naming convention": "obs-dashboard-naming",
    "Observability: log levels in production": "obs-log-levels",
    "Observability: how on-call rotation works": "obs-oncall-rotation",
    "Observability: what the error budget is for": "obs-error-budget",
    "Observability: what counts as a user-facing error": "obs-user-facing-err",
    "Observability: how request IDs work": "obs-request-id",
    "Testing: how integration tests are organized": "testing-integration-org",
    "Testing: when to use a fixture vs a factory": "testing-fixture-vs-factory",
    "Testing: how to mock external HTTP calls": "testing-mock-http",
    "Testing: what the coverage gate is": "testing-coverage-gate",
    "Testing: how flaky tests are handled": "testing-flaky",
    "Testing: property-based testing with Hypothesis": "testing-property-based",
    "Testing: how snapshot tests work": "testing-snapshots",
    "Testing: how load tests are run": "testing-load",
    "Testing: how database tests use transactions": "testing-db-transactions",
    "Testing: how CI parallelizes": "testing-ci-parallel",
    "API: why GET /users returns paginated cursors": "api-pagination",
    "API: rate limiting per API key": "api-rate-limit",
    "API: bulk endpoints and their request shapes": "api-bulk",
    "API: how versioning works": "api-versioning",
    "API: the difference between 401 and 403": "api-401-vs-403",
    "API: why errors return RFC 7807 problem details": "api-problem-details",
    "API: what `expand` parameters do": "api-expand",
    "API: idempotency keys and request bodies": "api-idempotency",
    "API: how webhook signatures are verified": "api-webhook-sig",
    "API: graceful shutdown behavior": "api-graceful-shutdown",
    "Data model: why `id` is UUIDv7 not UUIDv4": "dm-uuidv7",
    "Data model: how soft deletes work": "dm-soft-delete",
    "Data model: time columns use TIMESTAMPTZ not TIMESTAMP": "dm-timestamptz",
    "Data model: how monetary amounts are stored": "dm-money-bigint",
    "Data model: why we don't use foreign key constraints": "dm-no-fk",
}
assert len(TITLE_TO_SLUG) == 75, f"expected 75 entries, got {len(TITLE_TO_SLUG)}"
