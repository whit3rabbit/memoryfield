ROADMAP.md 5.1/5.2: built mf mcp, a stdio MCP server (mf/mcp_server.py) wrapping search/read/write/raw_add, each a thin call into the same library function the CLI uses. Confirmed via Context7 before writing: the mcp Python SDK v2 renamed FastMCP to MCPServer and moved the import path, so `from mcp.server.fastmcp import FastMCP` (the widely-documented v1 API) is a dead stub in mcp>=2 that raises ModuleNotFoundError, not a working import -- use `from mcp.server import MCPServer` instead, and verify the installed SDK version before trusting any mcp code sample. Separately, uv build's sdist had no [tool.hatch.build.targets.sdist] scoping, so hatchling's "everything not gitignored" default bundled the whole repo (157-page eval corpus, every doc, and notes/raw/'s own private session extracts) into the source distribution -- fixed with only-include = ["mf"], 374 files/559 KB down to 29 files/53 KB. Measured mcp's real install cost by building the field twice (with/without) into a scratch UV_TOOL_DIR: 30 packages without it, 44 with it, so mcp alone pulls in 14 packages (cryptography, starlette, uvicorn, pydantic, and their own trees) nothing in the core search/read/write path touches. Asked the user rather than assuming; moved mcp to an optional [mcp] extra instead of a core dependency, with mf mcp exiting 1 with an install-command message instead of an ImportError traceback when the extra is missing. 5.2 closed with this scope; versioning, a CI publish workflow, and cross-platform install verification are deliberately deferred, gated on a real git remote and an actual publish decision, neither of which exist yet.
